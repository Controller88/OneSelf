# UNIFY ID — Next.js App Router Scaffold

## Instruções Rápidas

1.  **Instalação:** Instale as dependências com `npm install`.
2.  **Configuração:** Crie o arquivo `.env.local` e preencha as chaves secretas (Supabase, Google, etc.).
3.  **Execução:** Rode localmente com `npm run dev`.
